import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
  backBtn: {
    position: "relative",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    minWidth: 0,
    width: "26px",
    height: "26px",
    margin: "0 6px 0 0",
    border: "none",
    boxShadow: "none",
    padding: "0",
    color: "#98A2B3",
  },
});
